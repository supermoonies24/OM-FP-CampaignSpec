import { PhasePlaceholder } from "@/components/workflow/PhasePlaceholder";

export default function AdminTemplatesPage() {
  return (
    <PhasePlaceholder
      title="Templates"
      phase="Phase 2"
      description="Brief deck templates and email send templates used by the AI brief generator and the spec form pre-fill."
    />
  );
}
